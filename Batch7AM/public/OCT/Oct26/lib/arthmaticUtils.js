/**
 * Method to perform multiplication of values, takes 2 params
 */
function multiValues(firstVal, secondVal) {
	count++;
	var result;
	result = firstVal * secondVal;
	return result;
}

/**
 * Method to perform substraction of values, takes 2 params
 */
function subValues(firstVal, secondVal) {
	count++;
	var result;
	result = firstVal - secondVal;
	return result;
}

/**
 * Method to perform division  of values, takes 2 params
 */
function divValues(firstVal, secondVal) {
	count++;
	var result;
	result = firstVal / secondVal;
	return result;
}

/**
 * Method to perform adding of values, takes 2 params
 */
function addValues(firstVal, secondVal) {
	count++;
	var result;
	result = firstVal + secondVal;
	return result;
}